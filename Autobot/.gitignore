# Cache Python, nunca fonte
__pycache__/
*.py[cod]
.mypy_cache/
.pytest_cache/

# Sua conta real, nunca a minha nem a de mais ninguem
_conta_real.json
_custo_nativo.json

# Sua escolha de ativos, se voce gravar uma (descobrir_ativos.py --gravar)
campanha_ativos.json

# Resultados de campanha e logs sao dados de UMA maquina/conta, nao codigo
campanha_resultados*.jsonl
amostra_noite_resultados.jsonl
*.log
*.bak
*.bak_*

# Estado de runtime do dashboard (lockfile do processo, perfil sincronizado
# por ultimo) -- de UMA maquina, nao codigo
campanha_dashboard.lock.json
perfil_dashboard.json
